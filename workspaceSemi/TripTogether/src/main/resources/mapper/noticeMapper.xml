<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.triptogether.dao.NoticeDAO">
    <select id="allSelect" resultType="com.triptogether.vo.NoticeVO"><!-- 공지사항 나열 쿼리문 -->
        select no, title, id, date_format(writedate, '%Y.%m.%d') writedate from board  where u_type=2 order by no desc        
    </select>
    <insert id="noticeInsert">
        insert into board(id, title, content)
        values(#{id}, #{title}, #{content})
    </insert>
    <select id="noticeSelect" resultType="com.triptogether.vo.NoticeVO">
        select no, title, id, date_format(writedate, '%Y.%m.%d') writedate, content from board where no=${param1}
    </select>
    <update id="noticeUpdate">
        update board set title=#{title}, content=#{content} where no=#{no} and id=#{id}
    </update>
    <delete id="noticeDelete">
        delete from board where no=#{param1} and id=#{param2}
    </delete>
</mapper>