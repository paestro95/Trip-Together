<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="com.triptogether.dao.CommunityDAO">
	<select id="allSelect" resultType="CommunityVO"><!-- 공지사항 나열 쿼리문 -->
		select no, title, id, writedate from board order by no desc		
	</select>
	<insert id="communityInsert">
		insert into board(id, title, content)
		values(#{id}, #{title}, #{content})
	</insert>
	<select id="communitySelect" resultType="CommunityVO">
		select no, title, id, writedate from board where no=${param1}
	</select>
	<update id="communityUpdate">
		update board set title=#{title}, content=#{content} where no=#{no} and id=#{id}
	</update>
	<delete id="communityDelete">
		delete from board where no=#{param1} and id=#{param2}
	</delete>
</mapper>